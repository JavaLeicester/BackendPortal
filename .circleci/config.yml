version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk
      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4
    working_directory: ~/repo
    environment:
      # Customize the JVM maximum heap limit
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      # Download and cache dependencies
      #- restore_cache:
      #    keys:
      #    - v1-dependencies-{{ checksum "build.gradle" }}
          # fallback to using the latest cache if no exact match is found
       #   - v1-dependencies-
     # - run: gradle dependencies
     # - save_cache:
     #     paths:
     #       - ~/.gradle
     #     key: v1-dependencies-{{ checksum "build.gradle" }}
      # run tests!
      # - run: gradle test
      # - run: gradle check
     # - store_artifacts:
     #     path: build/reports/
 
    deploy:
           machine:
             enabled: true
           steps:
             - add_ssh_keys:
                 fingerprints:
                    - 7f:44:20:e1:23:52:60:90:3d:8b:81:2f:c9:7d:20:a0
             - run:
                name: Deploy Over SSH
                command: |
                  ssh -v root@46.101.34.160 "/var/local/deployScripts/./deployBe.sh"

 # ///////////
 # version: 2
 # jobs:
 #   build:
 #     docker:
 #       - image: debian:stretch
 #     steps:
 #       - checkout
      # Download and cache dependencies
 #       - run:
      # run tests!
      # - run: gradle test 
      # - run: gradle build
 #       - store_artifacts: 
 #           path: build/reports/
 # deploy:
 #      machine:
 #       enabled: true
 #      steps:
 #        - add_ssh_keys:
 #            fingerprints:
 #              - d0:aa:74:f9:60:dc:82:fb:e7:f4:2f:ef:30:f8:ff:f7
 #        - run:
 #            name: Deploy BackEndBookingAndCollection 
 #            command: |
 #              ssh -v root@178.128.170.227 "/var/local/deployScripts/./deployBe.sh"              